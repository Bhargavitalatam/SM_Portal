services:
  - type: web
    name: sm-portal-app
    runtime: docker
    plan: free
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
      - key: DATABASE_URL
        fromDatabase:
          name: sm-portal-db
          property: connectionString
      - key: DB_SSL
        value: "true"
      - key: REDIS_HOST
        fromService:
          type: redis
          name: sm-portal-redis
          property: host
      - key: REDIS_PORT
        fromService:
          type: redis
          name: sm-portal-redis
          property: port
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_EXPIRES_IN
        value: 1h
      - key: SEED_ON_START
        value: "true"
      - key: ADMIN_EMAIL
        value: admin@grantportal.com
      - key: ADMIN_PASSWORD
        value: Admin@123456

  - type: redis
    name: sm-portal-redis
    plan: free
    ipAllowList: []

databases:
  - name: sm-portal-db
    databaseName: gmpdb
    user: gmpuser
    plan: free
